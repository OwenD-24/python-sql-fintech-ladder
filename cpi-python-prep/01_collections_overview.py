# A list is an ordered, changeable collection.
amounts = [100, 250, 400]

# A tuple is an ordered, unchangeable collection.
currencies = ("GBP", "USD", "EUR")

# A dictionary stores key-value pairs, which is useful for labelled data.
transaction = {"id": "TXN001", "amount": 250, "currency": "GBP"}

# A set stores unique values, which is useful for duplicate checking.
seen_ids = {"TXN001", "TXN002"}